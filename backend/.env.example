PORT=4000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=gdgoc_sch

# JWT 서명 키 — 배포 시 반드시 무작위 값으로 교체
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Google AI Studio에서 발급 (무료 티어)
GEMINI_API_KEY=
# 무료 티어 모델. 비우면 gemini-2.5-flash 사용
GEMINI_MODEL=
# gemini | stub (비우면 키 유무로 자동 결정. 키가 없으면 stub으로 동작)
LLM_PROVIDER=
